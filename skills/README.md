# Skills

Each subdirectory is one Agent Skill.

Start new skills by copying `skills/_template/` and replacing the placeholder text
in `SKILL.md`.

Directories whose names start with `_` are templates or internal support
directories and are not synced to local agent skill directories.
